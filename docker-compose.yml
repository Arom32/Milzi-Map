services:
  yolo-app:
    build: .
    container_name: MilziMap
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    restart: always